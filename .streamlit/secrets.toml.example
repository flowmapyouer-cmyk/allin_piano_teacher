# 이 파일을 복사해서 .streamlit/secrets.toml 로 저장한 뒤 값을 채우세요.
# secrets.toml은 git에 커밋되지 않습니다 (.gitignore 처리됨).
# Streamlit Community Cloud에 배포할 때는 앱 설정 > Secrets 에 동일한 내용을 붙여넣으면 됩니다.

[supabase]
url = "https://YOUR_PROJECT_REF.supabase.co"
anon_key = "YOUR_SUPABASE_ANON_PUBLIC_KEY"
